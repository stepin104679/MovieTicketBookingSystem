/**
	* @file user.h
	*/
#ifndef __USER_H__
#define __USER_H__
/**
	* Handles user login information
	* @param[in] uname user name
	* @param[in] pword password
	* @return Login successful or not
	*/

int login_user(char* uname, char* pword);

#endif

